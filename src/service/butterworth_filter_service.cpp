#include "../../include/service/filter_service.h"

class ButterworthFilterService : public IFilterService {
public:
    std::vector<SignalDatapoint> Filter(std::vector<SignalDatapoint> values) override {
        // TODO(Ola Szota): trzeba uzupełnić
    }
};
